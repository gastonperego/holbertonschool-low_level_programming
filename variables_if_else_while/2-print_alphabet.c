#include <stdio.h>
/**
 * main- Display the alphabet in lowercase, followed by a new line
 *
 * Return: always zero
 */
int main(void)
{
	int ch;
	char z = z;

	for (ch = 'a'; ch < z)
	{
		putchar(a++);
	}
	return (0);
}
