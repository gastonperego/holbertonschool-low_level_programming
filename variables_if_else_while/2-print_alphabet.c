#include <stdio.h>
/**
 * main- Display the alphabet in lowercase, followed by a new line
 *
 * Return: always zero
 */
int main(void)
{
	for (char c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	return (0);
}
