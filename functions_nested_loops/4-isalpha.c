#include "main.h"
/**
 * _isalpha- Prints 1 if the input is a letter, prints 0 otherwise
 *
 * @c: Is the input
 *
 * Return: 0
 */
int _isalpha(int c)
{
	if ((c <= 90 && c >= 65) || (c <= 122 && c >= 97))
	{
		return ('1');
	}
	return ('0');
}
