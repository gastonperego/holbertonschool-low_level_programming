nclude "main.h"
/**
 * puts2- print the pair "lockers" of a string
 *
 * @str: string
 *
 * Return: 0
 */
void puts2(char *str);
{
	int c;

	for (c = 0; str[c] != '\0';)
	{
		c++
	}
	while (0 < c)
	{
		_putchar(str[c]);
		c+2;
	}
}
