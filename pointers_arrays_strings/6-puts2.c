#include "main.h"
/**
 * puts2- print the pair "lockers" of a string
 *
 * @str: string
 *
 * Return: 0
 */
void puts2(char *str)
{
	int c;
	int i = 0;

	for (c = 0; str[c] != '\0';)
	{
		c++
	}
	while (i < c)
	{
		_putchar(str[c]);
		i += 2;
	}
}
