#include "main.h"
/**
 * _puts_recursion-  prints a string, followed by a new line
 *
 * @s: string
 *
 * Return: void
 */
void _puts_recursion(char *s)
{
	int count = 0;

	if (s[count != '0'])
	{
		_putchar(s[count];
		_putchar(s[count + 1]);
	}
}
